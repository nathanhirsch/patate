<div class="mp">
  <div class="mp-wrap mp-page">
    <div class="mp-page__head">
      <p class="mp-mono mp-mono--accent">Morizot Passage</p>
      <h1 class="mp-page__title">{{ page.title | escape }}</h1>
    </div>
    <div class="mp-prose">{{ page.content }}</div>
  </div>
</div>
